@using System.Globalization
@{
    ViewBag.Title = "add";
}
<!-- Start XP Breadcrumbbar -->
<div class="xp-breadcrumbbar">
    <div class="row">
        <div class="col-md-6 col-lg-6">
            <h4 class="xp-page-title">Rolling Mill</h4>
        </div>
        <div class="col-md-6 col-lg-6">
            <div class="xp-breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")"><i class="icon-home"></i></a></li>
                    <li class="breadcrumb-item"><a href="#">Rolling Mill</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Add Details</li>
                </ol>
            </div>
        </div>
    </div>
</div>
<!-- End XP Breadcrumbbar -->
<div class="xp-contentbar">
    <form action="/RollingMill/AddDetails" id="addprofileform" method="post">

        <div class="row">
            <div class="col-lg-12">
                <div class="card m-b-30">
                    <div class="card-header bg-white text-right">
                        <button type="submit" class="btn btn-primary">Save Details</button>
                    </div>
                    <div class="card-body">
                        <div class="form-row">
                            <div class="form-group col-md-2">
                                <label>Date</label>
                                <input type="date" class="form-control" id="date" name="Date" readonly />
                            </div>

                            <div class="form-group col-md-2">
                                <label>Plant</label>
                                <select class="form-control" name="Plant">
                                    <option selected disabled>-- please select --</option>
                                    <option value="Rolling Mill 1">Rolling Mill 1</option>
                                    <option value="Rolling Mill 2">Rolling Mill 2</option>
                                </select>
                            </div>

                            <div class="form-group col-md-2">
                                <label>Team</label>
                                <select class="form-control" name="Team">
                                    <option selected disabled>-- please select --</option>
                                    <option value="Team A">Team A</option>
                                    <option value="Team B">Team B</option>
                                    <option value="Team C">Team C</option>
                                </select>
                            </div>

                            <div class="form-group col-md-2">
                                <label>Shift</label>
                                <select class="form-control" name="Shift">
                                    <option selected disabled>-- please select --</option>
                                    <option value="Morning">Morning</option>
                                    <option value="Evening">Evening</option>
                                    <option value="Night">Night</option>
                                    <option value="Long Morning">Long Morning</option>
                                    <option value="Long Night">Long Night</option>
                                </select>
                            </div>

                            <div class="form-group col-md-2">
                                <label>Shift Incharge</label>
                                <select class="form-control" name="ShiftIncharge">
                                    <option selected disabled>-- please select --</option>
                                    <option value="Muhammad Ali">Muhammad Ali</option>
                                    <option value="Altaf Khan">Altaf Khan</option>
                                    <option value="Waseem Akram">Waseem Akram</option>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>


<!-- 1️⃣ jQuery (ONLY ONE, no duplicates) -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<!-- 2️⃣ Popper (Bootstrap dependency) -->
<script src="~/assets/js/popper.min.js"></script>

<!-- 3️⃣ Bootstrap JS (THIS DEFINES `bootstrap`) -->
<script src="~/assets/js/bootstrap.min.js"></script>

<!-- 4️⃣ Bootstrap Select -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap-select@1.14.0-beta3/dist/js/bootstrap-select.min.js"></script>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

@*<script>
        $(document).ready(function () {
            // Set today date
            $("#date").val(new Date().toISOString().split('T')[0]);
        });
    </script>*@

<script>
    $(document).ready(function () {

        // Set today date
        $("#date").val(new Date().toISOString().split('T')[0]);

        let isSubmitting = false;   // 🔐 double-submit lock

        $("#addprofileform").on("submit", function (e) {
            e.preventDefault(); // ❗ pehle submit roko

            if (isSubmitting) return; // already submitting

            Swal.fire({
                title: 'Confirm Submission',
                text: 'Are you sure you want to submit Record?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Yes, Submit',
                cancelButtonText: 'Cancel',
                allowOutsideClick: false,
                allowEscapeKey: false
            }).then((result) => {

                if (result.isConfirmed) {

                    isSubmitting = true; // 🔒 lock

                    // Disable submit button
                    $("#addprofileform")
                        .find("button[type='submit']")
                        .prop("disabled", true)
                        .text("Submitting...");

                    // Finally submit form
                    $("#addprofileform")[0].submit();
                }
            });
        });

    });
</script>