@*@model DAL.Models.BilletBoardBLL*@
@{
    ViewBag.Title = "add";
}

<div class="xp-breadcrumbbar">
    <div class="row">
        <div class="col-md-6 col-lg-6">
            <h4 class="xp-page-title">Billet Board</h4>
        </div>
        <div class="col-md-6 col-lg-6">
            <div class="xp-breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index" ,"Home")"><i class="icon-home"></i></a></li>
                    <li class="breadcrumb-item"><a href="#">Billet Board</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Add Billet Boarding</li>
                </ol>
            </div>
        </div>
    </div>
</div>
<style>
    .dropdown-container {
        position: relative;
    }

    .dropdown-menu-custom {
        position: absolute;
        top: 100%;
        left: 0;
        z-index: 1000;
        width: 100%;
        max-width: 500px;
        /* 💥 INCREASE THIS VALUE 💥 */
        max-height: 250px; /* Increased height */
        overflow-y: auto; /* Keep scrollbar */
        border: 1px solid #ccc;
        background-color: white;
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.175);
        font-size: 12px;
    }

        .dropdown-menu-custom table {
            table-layout: fixed; /* Ensures fixed column widths */
        }

        .dropdown-menu-custom tbody tr {
            cursor: pointer;
        }

            .dropdown-menu-custom tbody tr:hover {
                background-color: #f5f5f5;
            }
            /* Style for selected row, if needed */
            .dropdown-menu-custom tbody tr.selected {
                background-color: #007bff;
                color: white;
            }

        /* Ensures headers are bold */
        .dropdown-menu-custom th {
            font-weight: bold;
            /* Add a border to the right of the header cells for alignment */
            border-right: 1px solid #ccc;
        }

            /* Remove the border from the last header cell */
            .dropdown-menu-custom th:last-child {
                border-right: none;
            }

        /* 💥 VERTICAL AND HORIZONTAL SEPARATORS 💥 */
        .dropdown-menu-custom td {
            /* Horizontal Separator (bottom) */
            border-bottom: 1px solid #ddd;
            /* Vertical Separator (right) */
            border-right: 1px solid #ccc; /* Light vertical line between columns */
        }

            /* Ensure the last column (last td in each row) does not have a right border */
            .dropdown-menu-custom td:last-child {
                border-right: none;
            }

        /* Ensure the last row doesn't have a bottom border, keeping it clean */
        .dropdown-menu-custom tr:last-child td {
            border-bottom: none;
        }
    /* ... (Keep existing hover styles) ... */
</style>

<div class="xp-contentbar">
    <form action="/Quality/addbillet" id="addprofileform" method="post" enctype="multipart/form-data">

        <div class="row">
            <div class="col-lg-12">
                <div class="card m-b-30">
                    <div class="card-header bg-white" style="ALIGN-SELF: end;">
                        <button type="submit" class="btn btn-primary">Submit</button>
                    </div>
                    <div class="card-body">
                        <div class="form-row">
                            <div class="form-group col-md-3">
                                <label for="date">Date</label>
                                <input type="date" class="form-control" id="date" name="date" placeholder="Date">
                            </div>
                            <div class="form-group col-md-3">
                                <label for="billetboarding">Billet Boarding #</label>
                                <input type="text" class="form-control" id="billetboarding" name="billetboarding" placeholder="">
                            </div>
                            <div class="form-group col-md-3">
                                <label for="plantname">Plant</label>
                                <select class="form-control" id="plantname" name="plantname">
                                    <option disabled selected>Please Select</option>
                                    <option value="RM1">RM1</option>
                                    <option value="RM2">RM2</option>
                                </select>
                            </div>
                            <div class="form-group col-md-3">
                                <label for="shift">Shift</label>
                                <select class="form-control" id="shift" name="shift">
                                    <option disabled selected>Please Select</option>
                                    <option value="Morning">Morning</option>
                                    <option value="Long Morning">Long Morning</option>
                                    <option value="Evening">Evening</option>
                                    <option value="Night">Night</option>
                                    <option value="Long Night">Long Night</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-row">
                            <div class="form-group col-md-3 dropdown-container">
                                <label for="productSpec">Product Spec/Grade</label>
                                @* 2. VISIBLE INPUT FIELD *@
                                <div class="input-group">
                                    <input type="text"
                                           id="productSpec"
                                           class="form-control"
                                           placeholder="-- Select Product Grade --"
                                           readonly
                                           name="productspecs">
                                    <div class="input-group-append">
                                        <button class="btn btn-outline-secondary" type="button" id="toggleDropdownBtn">
                                            <i class="fa fa-caret-down"></i>
                                        </button>
                                    </div>
                                </div>

                                @* 3. CUSTOM MULTI-COLUMN DROPDOWN TABLE *@
                                <div id="multiColumnDropdown" class="dropdown-menu-custom shadow" style="display: none;">
                                    <table class="table table-hover table-sm mb-0">
                                        <thead>
                                            <tr>
                                                <th style="width: 30%;">Spec/Grade</th>
                                                <th style="width: 30%;">Steel Grade</th>
                                                <th style="width: 10%;">L</th>
                                                <th style="width: 10%;">W</th>
                                                <th style="width: 20%;">Billet Length (m)</th>
                                            </tr>
                                        </thead>
                                        <tbody id="specGradeBody">
                                            @* DATA ROWS WILL BE POPULATED BY JAVASCRIPT *@
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                            @* 4. ASSOCIATED FIELDS (Billet Length) *@
                            <div class="form-group col-md-3">
                                <label for="billetLength">Billet Length (m)</label>
                                <input type="text" id="billetLength" class="form-control" name="billetlength" readonly>
                            </div>
                            @* Add other associated fields here (L, W, Steel Grade) *@
                            <div class="form-group col-md-3">
                                <label for="crosssection">Cross Section (mm)</label>
                                <input type="text" id="crosssection" class="form-control" name="crosssection" readonly>
                            </div>
                            <div class="form-group col-md-3">
                                <label for="steelGrade">Grade</label>
                                <input type="text" id="steelGrade" class="form-control" name="steelGrade" readonly>
                            </div>
                        </div>

                        <div class="form-row">
                            
                            <div class="form-group col-md-3">
                                <label for="size">Size</label>
                                <select class="form-control" id="size" name="size">
                                    <option disabled selected>Please Select</option>
                                    <option value="08mm">08mm</option>
                                    <option value="10mm">10mm</option>
                                    <option value="12mm">12mm</option>
                                    <option value="14mm">14mm</option>
                                    <option value="16mm">16mm</option>
                                    <option value="18mm">18mm</option>
                                    <option value="20mm">20mm</option>
                                    <option value="22mm">22mm</option>
                                    <option value="25mm">25mm</option>
                                    <option value="28mm">28mm</option>
                                    <option value="30mm">30mm</option>
                                    <option value="32mm">32mm</option>
                                    <option value="40mm">40mm</option>
                                    <option value="09mm">09mm</option>
                                    <option value="5.5mm">5.5mm</option>
                                    <option value="6.5mm">6.5mm</option>
                                    <option value="07mm">07mm</option>
                                    <option value="11mm">11mm</option>
                                    <option value="7.5mm">7.5mm</option>
                                    <option value="36mm">36mm</option>
                                </select>
                            </div>

                            <div class="form-group col-md-3">
                                <label for="profile">Profile</label>
                                <select class="form-control" id="profile" name="profile">
                                    <option disabled selected>-- please select --</option>
                                    <option value="RB">RB</option>
                                    <option value="WR">WR</option>
                                    <option value="RC">RC</option>
                                    <option value="PB">PB</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-row">
                            <div class="form-group col-md-6 dropdown-container">
                                <label for="productSpec">Remarks</label>
                                <textarea class="form-control" rows="2" name="Remarks"></textarea>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                <div class="card m-b-30">
                    <div class="card-header bg-white">
                        <h5 class="card-title text-black">Chemical Analysis</h5>
                    </div>
                    <div class="card-body">

                        <div class="table-responsive m-b-30">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th scope="col">Heat #</th>
                                        <th scope="col">No. Of Billet</th>
                                        <th scope="col">C</th>
                                        <th scope="col">Si</th>
                                        <th scope="col">Mn</th>
                                        <th scope="col">S</th>
                                        <th scope="col">P</th>
                                        <th scope="col">N</th>
                                        <th scope="col">Ceq</th>
                                        <th scope="col">Status</th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <tbody id="chemBody">
                                    <!-- Dynamic rows will be added here -->
                                </tbody>
                                @*<tbody id="chemBody">
                                        @if (Model.Chemistry == null || !Model.chem.Any())
                                        {
                                            // default one row
                                            Model. = new List<DAL.Models.RMChemicalAnalysisBLL> { new DAL.Models.RMChemicalAnalysisBLL() };
                                        }

                                        @for (int i = 0; i < Model.Chemistry.Count; i++)
                                        {
                                            <tr>
                                                <td class="text-center">@(i + 1)</td>

                                                <td><input type="number" class="form-control" name="Chemistry[@i].NoOfBillets" value="@Model.Chemistry[i].NoOfBillets" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].C" step="0.01" value="@Model.Chemistry[i].C" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].Si" step="0.01" value="@Model.Chemistry[i].Si" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].Mn" step="0.01" value="@Model.Chemistry[i].Mn" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].S" step="0.01" value="@Model.Chemistry[i].S" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].P" step="0.01" value="@Model.Chemistry[i].P" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].N" step="0.01" value="@Model.Chemistry[i].N" /></td>
                                                <td><input type="number" class="form-control" name="Chemistry[@i].Ceq" step="0.01" value="@Model.Chemistry[i].Ceq" /></td>

                                                <td>
                                                    <select class="form-control" name="Chemistry[@i].HeatStatus">
                                                        <option disabled selected>-- select --</option>
                                                        <option value="Cancelled">Cancelled</option>
                                                        <option value="Charging">Charging</option>
                                                        <option value="Complete">Complete</option>
                                                        <option value="In Process">In-Process</option>
                                                        <option value="New">New</option>
                                                        <option value="Rolled">Rolled</option>
                                                        <option value="Rolling">Rolling</option>
                                                    </select>
                                                </td>

                                                <td class="text-center">
                                                    <button type="button" class="btn btn-sm btn-danger removeRow">X</button>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>*@
                            </table>
                            <button type="button" class="btn btn-sm btn-success" id="addChemRow">
                                + Add Row
                            </button>
                        </div>

                    </div>
                </div>
            </div>
        </div>

    </form>
</div>
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script>
    // Set current date
    const today = new Date().toISOString().split('T')[0];
    document.getElementById("date").value = today;

    // JAVASCRIPT FOR MULTI-COLUMN DROPDOWN
    $(document).ready(function () {

        const gradeDataJson = '@Html.Raw(ViewBag.GradeDataJson)';
        let gradeData = [];

        try {
            gradeData = JSON.parse(gradeDataJson);
        } catch (e) {
            console.error("Error parsing Grade Data JSON:", e);
            return;
        }

        const $specGradeBody = $('#specGradeBody');
        const $inputField = $('#productSpec');
        const $dropdownTable = $('#multiColumnDropdown');
        const $toggleBtn = $('#toggleDropdownBtn');
        const $hiddenIdField = $('#hiddenProductID');
        const $billetLengthField = $('#billetLength');
        const $crossSectionField = $('#crosssection');
        const $steelGradeField = $('#steelGrade');   // ✅ add this input in HTML

        // ----------------------------
        // POPULATE DROPDOWN TABLE
        // ----------------------------
        gradeData.forEach(item => {
            const row = `
                <tr
                    data-id="${item.ProductID}"
                    data-spec="${item.SpecGrade}"
                    data-grade="${item.SteelGrade}"
                    data-l="${item.Length}"
                    data-w="${item.Width}"
                    data-billet="${item.BilletLength}">
                    <td>${item.SpecGrade}</td>
                    <td>${item.SteelGrade}</td>
                    <td>${item.Length}</td>
                    <td>${item.Width}</td>
                    <td>${item.BilletLength}</td>
                </tr>`;
            $specGradeBody.append(row);
        });

        // ----------------------------
        // TOGGLE DROPDOWN
        // ----------------------------
        $toggleBtn.on('click', function () {
            $dropdownTable.toggle();
        });

        // ----------------------------
        // ROW CLICK EVENT
        // ----------------------------
        $specGradeBody.on('click', 'tr', function () {

            const $row = $(this);

            const selectedId = $row.data('id');
            const selectedSpec = $row.data('spec');
            const selectedGrade = $row.data('grade');
            const billetLength = $row.data('billet');

            const length = $row.data('l');
            const width = $row.data('w');
            const crossSectionValue = length + 'x' + width;

            // ✅ SET VALUES
            debugger
            $inputField.val(selectedSpec);
            $hiddenIdField.val(selectedId);
            $billetLengthField.val(billetLength);
            $crossSectionField.val(crossSectionValue);
            $steelGradeField.val(selectedGrade);     // ✅ Fixed variable

            $dropdownTable.hide();
        });

        // ----------------------------
        // HIDE DROPDOWN IF CLICK OUTSIDE
        // ----------------------------
        $(document).on('click', function (e) {
            if (!$dropdownTable.is(e.target) &&
                $dropdownTable.has(e.target).length === 0 &&
                !$toggleBtn.is(e.target) &&
                !$inputField.is(e.target)) {
                $dropdownTable.hide();
            }
        });
    });
</script>

<script>
    $(document).ready(function () {

        let rowIndex = 0;

        // Add first row by default
        addChemRow();

        // Add new row
        $('#addChemRow').click(function () {
            addChemRow();
        });

        // Remove row
        $('#chemBody').on('click', '.removeRow', function () {
            $(this).closest('tr').remove();
            reindexRows();
        });

        function addChemRow() {

            const row = `
        <tr>
            <td class="text-center">
            <input type="text" class="form-control" id="" name="Chemistry[${rowIndex}].HeatNo" placeholder="">
            </td>

            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].NoOfBillets" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].C" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].Si" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].Mn" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].S" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].P" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].N" /></td>
            <td><input type="text" class="form-control" name="Chemistry[${rowIndex}].Ceq" /></td>

            <td>
                <select class="form-control" name="Chemistry[${rowIndex}].HeatStatus">
                    <option value="">-- select --</option>
                    <option value="100">Complete</option>
                    <option value="101">Cancelled</option>
                    <option value="102">Charging</option>
                    <option value="103">In Process</option>
                    <option value="104">New</option>
                    <option value="105">Rolled</option>
                    <option value="106">Rolling</option>
                </select>
            </td>

            <td class="text-center">
                <button type="button" class="btn btn-sm btn-danger removeRow">X</button>
            </td>
        </tr>`;

            $('#chemBody').append(row);
            rowIndex++;
            reindexRows();
        }

        function reindexRows() {
            $('#chemBody tr').each(function (i) {
                $(this).find('.sr').text(i + 1);
            });
        }

    });
</script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    $(document).ready(function () {

        var errorMessage = '@Html.Raw(HttpUtility.JavaScriptStringEncode(Convert.ToString(TempData["ErrorMessage"])))';
        var successMessage = '@Html.Raw(HttpUtility.JavaScriptStringEncode(Convert.ToString(TempData["SuccessMessage"])))';

        if (errorMessage && errorMessage !== '') {
            Swal.fire({
                title: 'Error',
                text: errorMessage,
                icon: 'error',
                confirmButtonText: 'OK'
            });
        }

        if (successMessage && successMessage !== '') {
            Swal.fire({
                title: 'Success',
                text: successMessage,
                icon: 'success',
                confirmButtonText: 'OK'
            });
        }

    });
</script>
<script>
    $(document).ready(function () {

        let isSubmitting = false;

        $("#addprofileform").on("submit", function (e) {
            e.preventDefault();

            if (isSubmitting) return;

            var billetBoarding = $("#billetboarding").val().trim();

            if (billetBoarding === "") {
                Swal.fire({
                    title: 'Error',
                    text: 'Billet Boarding number is required.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return;
            }

            var heatNos = [];
            var duplicateHeatNos = [];

            $("#chemBody").find("input[name*='.HeatNo']").each(function () {
                var heatNo = $(this).val().trim().toUpperCase();

                if (heatNo !== "") {
                    if (heatNos.indexOf(heatNo) >= 0) {
                        duplicateHeatNos.push(heatNo);
                    } else {
                        heatNos.push(heatNo);
                    }
                }
            });

            if (heatNos.length === 0) {
                Swal.fire({
                    title: 'Error',
                    text: 'Please enter at least one Heat No.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return;
            }

            if (duplicateHeatNos.length > 0) {
                Swal.fire({
                    title: 'Duplicate Heat No',
                    text: 'Duplicate Heat No found: ' + duplicateHeatNos.join(', '),
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return;
            }

            Swal.fire({
                title: 'Confirm Submission',
                text: 'Are you sure you want to submit this Billet Boarding?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Yes, Submit',
                cancelButtonText: 'Cancel',
                allowOutsideClick: false,
                allowEscapeKey: false
            }).then((result) => {

                if (result.isConfirmed) {

                    isSubmitting = true;

                    $("#addprofileform")
                        .find("button[type='submit']")
                        .prop("disabled", true)
                        .text("Submitting...");

                    $("#addprofileform")[0].submit();
                }
            });
        });

    });
</script>

@*<script>
    $(document).ready(function () {

        let isSubmitting = false;   // 🔐 double-submit lock

        $("#addprofileform").on("submit", function (e) {
            e.preventDefault(); // ❗ pehle submit roko

            if (isSubmitting) return; // already submitting

            Swal.fire({
                title: 'Confirm Submission',
                text: 'Are you sure you want to submit this Heat Record?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Yes, Submit',
                cancelButtonText: 'Cancel',
                allowOutsideClick: false,
                allowEscapeKey: false
            }).then((result) => {

                if (result.isConfirmed) {

                    isSubmitting = true; // 🔒 lock

                    // Disable submit button
                    $("#addprofileform")
                        .find("button[type='submit']")
                        .prop("disabled", true)
                        .text("Submitting...");

                    // Finally submit form
                    $("#addprofileform")[0].submit();
                }
            });
        });

    });
</script>*@
